Get USB version of CLI working

Bring in CLI commands from the Atmel/STM example:
  GPIO
  I2C
  SPI

Figure out MCU flash, get it to work

Get firmware updates over ymodem working.

Add to info command:
  Clock frequency
  Reset reason
  Memory size?
  Image address information
  Print board serial number
  etc.

Get reboot command to work
